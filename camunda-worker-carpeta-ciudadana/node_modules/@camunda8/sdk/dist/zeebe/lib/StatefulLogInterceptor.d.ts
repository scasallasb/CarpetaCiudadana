import { Characteristics, State } from '@camunda8/sdk/dist/zeebe/lib/ConnectionFactory';
import { ZBLoggerConfig } from '@camunda8/sdk/dist/zeebe/lib/interfaces-1.0';
import { ZBLogger } from '@camunda8/sdk/dist/zeebe/lib/ZBLogger';
export declare class StatefulLogInterceptor {
    characteristics: Characteristics;
    log: ZBLogger;
    blocking: boolean;
    state: State;
    errors: never[];
    logs: never[];
    initialConnection: boolean;
    private blockingTimer?;
    constructor({ characteristics, logConfig, }: {
        characteristics: Characteristics;
        logConfig: ZBLoggerConfig;
    });
    close(): void;
    logError: (err: any) => void;
    logInfo: (msg: any) => void;
    logDebug: (msg: any, ...args: any[]) => void;
    logDirect: (msg: any) => false | void;
    connectionError: () => void;
    ready: () => void;
    private emptyErrors;
    private emptyLogs;
    private wrap;
    private info;
    private error;
}
